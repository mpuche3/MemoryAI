# Python dependencies for the MemoryAI ingestion tooling.
# Recreate the environment from the repository root with:
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
markitdown[all]>=0.1.6
pymupdf>=1.27
